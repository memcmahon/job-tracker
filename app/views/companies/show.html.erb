<section>
  <%= link_to "New Job", new_company_job_path(@company) %>
  <h2>All Jobs for <%= @company.name %></h2>

  <section class ="grid">
    <% @jobs.each do |job| %>
      <div>
        <%= link_to job.title, job_path(job) %> at <%= job.company.name %></ul>
      </div>
    <% end %>
  </section>

  <section>
    <div>

      <h3>Company Contacts:</h3>

      <% @contacts.each do |contact| %>
        <%= contact.full_name %>, <%= contact.position %><br>
        <%= contact.email %>
      <% end %>
    </div>

    <div>
      <h3>New Contact:</h3>

      <%= form_for [@company, @contact] do |f| %>
        <%= f.label :full_name %>
        <%= f.text_field :full_name %>

        <%= f.label :position %>
        <%= f.text_field :position %>

        <%= f.label :email %>
        <%= f.text_field :email %>

        <%= f.submit %>
      <% end %>
    </div>
  </section>
</section>
