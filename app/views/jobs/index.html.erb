All Jobs for <strong><%= @company.name %></strong>

<% @jobs.each do |job| %>
  <ul><%= job.title %> at <%= job.company.name %></ul>
  <ul> <%= link_to "Edit #{job.title}", edit_company_job_path(job.company, job) %>  </ul>
<% end %>
